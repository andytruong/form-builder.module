(function (angular, Drupal) {

    angular
            .module('FormBuilderEntityEditingForm')
            .factory('entity', 123)
            // .config()
            ;

    console.log(Drupal);

})(angular, Drupal);
